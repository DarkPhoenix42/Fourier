#include "Vector2D.hpp"
#include <vector>

using namespace std;

/**
 * @param numFrequenciees if this is k then the return value will have size 2*k+1
*/
vector<Vector2D> fourier_series(vector<Vector2D> points, int numFrequencies)
{
    
}